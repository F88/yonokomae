// DISABLED DUPLICATE: Use tests under '@/yk/repo/seed-system/seeds.validation.test.ts'
import { describe, it, expect } from 'vitest';

describe('duplicate disabled', () => {
  it('noop', () => {
    expect(true).toBe(true);
  });
});
