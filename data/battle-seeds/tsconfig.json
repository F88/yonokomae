// Seed data package tsconfig.
// All three seed packages share the same structure intentionally so that
// they can be built / published independently while remaining symmetrical.
// If you change one (battle-seeds, news-seeds, historical-evidence), reflect
// the change in the others or consider introducing a shared role config.
{
  "extends": "../../tsconfig.role.seed.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "**/*.test.ts"]
}
