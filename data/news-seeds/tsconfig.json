// See battle-seeds/tsconfig.json for rationale — kept mirrored intentionally.
{
  "extends": "../../tsconfig.role.seed.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "**/*.test.ts"]
}
